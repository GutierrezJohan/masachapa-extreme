{
  "scripts": {
    "start": "node src/server.js",
    "prisma:introspect": "prisma db pull --schema=./prisma/schema.prisma",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init --schema=./prisma/schema.prisma",
    "prisma:studio": "prisma studio",
    "seed:products": "node src/seed/seedProducts.js"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "prisma": "^6.19.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1"
  }
}
